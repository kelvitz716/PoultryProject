# Local-only source-build override. Never use this file on the OCI deployment
# host: production must run the immutable image pulled by CI.
services:
  poultry-dss:
    image: poultry-dss:local
    environment:
      NODE_ENV: development
    build:
      context: .
      dockerfile: Dockerfile
