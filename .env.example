# ──────────────────────────────────────────────────────────────────────────────
# Poultry DSS — Environment Configuration Template
# ──────────────────────────────────────────────────────────────────────────────

# Port to bind the HTTP server to (Default: 80)
PORT=80

# Environment mode ('development' or 'production')
NODE_ENV=development

# ── REQUIRED SECURITY CONFIGURATION ───────────────────────────────────────────
# Session secret used to sign session cookies.
# Generate a strong key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST NOT be empty or have the default value in any environment.
SESSION_SECRET=

# ── PAYMENT-IMPORT SMS WEBHOOK ────────────────────────────────────────────────
# At least 32 non-whitespace characters. Configure the matching value only in the
# trusted Android forwarder's HMAC setting; never expose it in UI, database, or logs.
PAYMENT_IMPORT_WEBHOOK_SECRET=
# Exact, case-insensitive comma-separated provider labels only; surrounding commas are trimmed.
# Empty, duplicate, wildcard, and regex entries are rejected. Example: MPESA, M-PESA
PAYMENT_IMPORT_ALLOWED_SENDERS=

# ── TUYA CLOUD TELEMETRY SYNCHRONIZATION ──────────────────────────────────────
# Credentials for the Tuya Smart Cloud API to sync environmental sensor history.
# Required if environmental sync is active.
TUYA_CLIENT_ID=
TUYA_CLIENT_SECRET=
TUYA_DEVICE_ID=
# Region code ('eu' for Europe/Africa, 'us' for America, 'cn' for China)
TUYA_REGION=eu

# ── TELEGRAM SENSOR ALERTS ────────────────────────────────────────────────────
# Telegram Bot integration for sensor offline/welfare alerts.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── END-TO-END AUTOMATED TESTING ──────────────────────────────────────────────
# Optional password seed for automated browser-test login. Normal app startup
# does not require this value.
E2E_TEST_PASSWORD=
